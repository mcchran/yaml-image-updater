{
  "name": "continuous_deployer",
  "version": "1.0.0",
  "description": "## Description `Continuous_deployer` should be triggered by repo dispatches to update all yaml files for any new image release. It requires a mapping of the form `repo->image`. Following it updates the image tag in the related yaml file with  the tag provided in the dispatched event.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "glob": "^10.3.10",
    "js-yaml": "^4.1.0"
  }
}
